/*

class SetTree:
  Constructor:
    // parameter
    data - null or iterable
    compareFunction - inequality function of key

  Method:
    // iterator
    begin
    end
    [Symbol.iterator]

    // Capacity
    empty
    size

    // Modifiers
    clear
    insert
    erase
    merge

    // Lookup
    count
    find
    contains
    equalRange
    lowerBound
    upperBound

    // Observers
    keyComp

*/

